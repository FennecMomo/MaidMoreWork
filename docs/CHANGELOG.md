# 版本更新记录

> **记录规则：只有作者明确要求"记录到本文档"时，才允许新增/修改条目。**
> 未明确指示时，任何改动一律不得写入本文档（包括 bug 修复、机制调整、临时诊断、内部重构等）。

> **版本号规则（2026-09-20 修订：按变更级别递增，不按打包发布）：**
> - **系统级**（跨模块/框架级改动）→ 第一位 +1，后两位归零（如 0.1.x → 1.0.0）；
> - **模块级**（新增模块或成体系功能）→ 第二位 +1，第三位归零（如 0.1.3 → 0.2.0）；
> - **修改级**（bug 修复、小调整）→ 第三位 +1（如 0.1.2 → 0.1.3）；
> - **模组版本号与本文档同步**：每次版本号变动时，同步更新 `gradle.properties` 的 `mod_version` 与 `docs/INDEX.md` 顶部的版本行。

---

## 0.1.3

- 增加矿井仓库的UI界面
- 为了防止酒狐老是掉下去，给楼梯加了墙

## 0.1.2

- 新增了矿道控制器和矿道，女仆现在能够挖掘水平矿道了，或者通过矿道控制器在矿井中快速移动

## 0.1.1

- 酒狐在挖矿时可以处理大范围的水域了
