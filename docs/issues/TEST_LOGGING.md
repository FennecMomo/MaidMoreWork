# MaidMoreWork 测试清单

> 当前轮次：「合成表与类型限制 (2026-09-22)」；矿井仓库 UI 轮次待测项仍有效（见下一节）
> 历史遗留：砍树轮次 #5 / #10 未复测（相关机制已重写），#9 已修（7c97ab4）——遗留项见 [../../../docs/TODO.md](../../../docs/TODO.md)

---

## 合成表与类型限制 (2026-09-22)

### 本轮待测

| # | 测试项 | 结果 | 备注 |
|---|--------|------|------|
| 1 | 工程中心合成 | □ | 8 木板围一圈 + 中心任意石质工具（石镐/斧/锹/锄/剑） |
| 2 | 标记工具合成 | □ | 木棍在左上(7)/中心(5) + 任意原木在右下(3) |
| 3 | 矿井标记工具合成 | □ | 木棍在左上(7)/中心(5) + 铜锭在右下(3) |
| 4 | 工程中心激活页不再出现"采矿"类型 | □ | 放置中心 → 绑定标记 → 右键空气，只应能选砍树 |
| 5 | 矿井中心直达配置页、不弹类型选择 | □ | 标记右键矿井方块 → 直接编辑半径/锚点/命名 |
| 6 | `maidmorework center type <id> mining` 被拒绝 | □ | 应提示"该类型不能在工程中心中选择" |

### 操作人反馈（2026-09-22，待修，今日不改）

| # | 问题 | 现象 | 初步定位 |
|---|------|------|---------|
| N1 | 挖矿道时墙上的矿没被替换成垫脚方块 | 女仆只挖矿道几何内的方块，保留位（墙）上的矿未走 REPLACE | REPLACE 收窄为仅矿物后，矿道工程挖格/保留位扫描可能未覆盖墙面矿（根因待查） |
| N2 | 绑定矿井中心后不显示矿井范围 | 用标记工具蹲下+右键绑定矿井中心，无边界线框 | 绑定流程（`handleCenterBind`）只写绑定 ID，未同步 `boundaryVisible`；矿井身份默认 `boundaryVisible=false`，客户端无边界数据（根因待查） |
| N3 | 工程中心没有储物空间，伐木女仆不放产物 | 普通工程中心无仓库面板；砍树完成后不掉落/存放进中心 | 仓库目前仅矿井（`MineInstance` + `WarehouseMenu`），基类中心无仓库；砍树存放逻辑未接中心（根因待查） |
| N4 | 女仆远走时工程中心卸载再加载有概率把女仆踢出 | 砍树女仆距离较远、中心区块卸载重载后，女仆与中心解绑 | 疑似参与者清理按"实体是否在场/可解析"判定，区块卸载期间的缺场被误判（根因待查） |

---

## 矿井仓库 UI 测试 (2026-09-20)

### 上轮反馈（已修，待验证）

| # | 问题 | 状态 | 说明 |
|---|------|------|------|
| W1 | 角标不显示真实总数，只显示原版 64 + 一层黑色遮罩 | ⚠ 已修待验证 | 旧实现角标文字 `0xFFFFFF`（alpha=0）被 26.x `GuiGraphicsExtractor.text` 跳过不渲染，只剩黑罩；改用原版 `renderSlotContents` 的 countText 替换堆数量 |
| W2 | 总数 >32767 数据槽同步截断（潜在） | ⚠ 已修待验证 | `ClientboundContainerSetDataPacket` 用 writeShort；计数数据槽拆 lo/hi 共 92 槽 |

### 本轮待测

| # | 测试项 | 结果 | 备注 |
|---|--------|------|------|
| 1 | <1000 角标显示精确数字 | □ | 如 500 |
| 2 | 缩写角标（截断不进位） | □ | 角标 0.5x 字号；1500→1.5k、12345→12k、2000000→2.0M、2147483647→2.1B |
| 3 | 悬停 tooltip 追加"总计 N"精确值 | □ | 灰色行 |
| 4 | 无黑色遮罩、不再显示 64 | □ | |
| 5 | 总数 >32767（如 40000）角标正确 | □ | |
| 6 | 翻页 / 左键取组 / 右键取半 / 拿物品存入 / Shift 双向转移回归 | □ | 翻页为两侧真按钮（不可翻置灰）+ 居中页码文本 |

---

# 历史轮次（归档）

## 砍树测试结果 (2026-07-25)

| # | 测试项 | 结果 | 备注 |
|---|--------|------|------|
| 4 | 参与人数正常 | ✓ | |
| 5 | 女仆收起放出后右上角 HUD 仍存在 | ✗ 待修复 | 空包拦截已修复，但工程变孤儿后要等 60 tick 清理才删，收起瞬间 HUD 残留。需在 release() 参与者归零时立刻删工程同步 HUD。 |
| 6 | 不同材质斧子砍树速度是否与原版破坏时间成比例 | ✓ | 进度增速与工具相关，正常 |
| 7 | 工作进度 0-1 反复跳，树砍不掉 | ✓ | WORK_ACTION 移至 ChopBehavior.start 写入后正常 |
| 8 | 不抢任务 | ✓ | |
| 9（原） | 砍树过程中女仆是否被掉落的树苗/木棍吸引，中断砍树跑去拾取 | ✗ 待修复 | 拾取阻断只拦了"捡"没拦"走过去"。女仆仍会中断砍树导航到掉落物旁边再回头，工程多次中断效率极差。需从源头阻止拾取意图。 |
| 10（其他1） | 首次放出先输出"家园没有可用目标"再找树 | ✗ 待修复 | 首帧不显示了，但当固定范围也找不到树时气泡也不显示了。handleSpiralExhausted 的条件判断需修复。 |
| 11（其他2） | 抵达创建工程后有概率一刀没砍换目标 | ✓ | 无法复现，距离改平面 XZ 后未再出现 |
| 12（工具磨损） | 砍树过程中斧子耐久是否正常扣减 | ✓ | 耐久随进度正常扣减 |
| 13（8新） | 砍树过程中给女仆背包塞更好斧子，是否自动换上 | ✓ | 工具自动切换，最多 10 tick |
| 14（9新） | 斧子放副手是否被检测到并换上 | ✓ | 副手工具正常检测并切换 |
| 15 | 工具磨损逻辑已恢复正常 | ✓ | accumulateToolWear 还原后正常 |

### 待修复（2026-07-25）

| # | 问题 | 说明 |
|---|------|------|
| 5 | 收起女仆时 HUD 不立刻消失 | release() 参与者归零时需立刻删工程同步 HUD，不等 60 tick 周期 |
| 9 | 砍树中被掉落物吸引中断 | 需从源头阻止女仆产生拾取意图，而非只拦捡这个动作（后续已修，见 7c97ab4） |
| 10 | 气泡该显示时不显示 | 固定范围搜索耗尽后应弹气泡，当前条件判断有问题 |

### 本轮变更 (2026-07-25)

| # | 改动 | 位置 |
|---|------|------|
| 1 | HUD 空包拦截导致客户端不清数据 | `ProjectManager.syncHudToPlayers` — 删除 `if empty return` |
| 2 | WORK_ACTION 被 TLM 清理导致 isActive 失败 | `LoggingTask` — 移出 createBrainTasks；`ChopBehavior.start` — 补写；`scanForTree` — 补写 WORK_TARGET |
| 3 | 工具磨损还原 | `CountingProject.accumulateToolWear` — 取消注释 |
| 4 | 首次放出弹无目标气泡 | `SearchBehavior.expandSpiral` — 原点不在家时不用家园限制 |
| 5 | 距离判断含 Y 轴误删工程 | `ChopBehavior` tickNavigate/tickChopLogs — 距离改平面 XZ |
| 6 | 不检查更好工具 / 不扫副手 | `ChopBehavior.equipAxe` — 重写为全扫挑最快 + 每次 execute 重检 |

## 工程中心测试 (2026-07-29)

### 测试结果

| # | 测试项 | 结果 | 备注 |
|---|--------|------|------|
| 1 | 放置方块后右键弹出激活面板 | ✓ | |
| 2 | 激活面板类型切换 | ✓ | [<][>] 按钮正常切换 |
| 3 | 确认后激活，边界出现 | ✓ | type 命令生效，boundaryVisible=true |
| 4 | 激活后右键弹出编辑面板 | ✓ | radius/anchor 正常编辑 |
| 5 | 标记工具右键激活后面板 | ✓ | |
| 6 | 激活面板显示图标 | ✗ | 当前只渲染文本，未渲染 icon() |
| 7 | 激活面板显示介绍 | ✗ | description 未渲染 |
| 8 | 类型切换按钮边界禁用 | ✗ | 仅1个类型时左右按钮仍可点击，应首个禁用左、末个禁用右 |
| 9 | 右键聊天信息 | ✗ | 现在右键一律弹出面板，之前设计是标记工具右键才弹面板，其他情况输出聊天信息 |

### 待修复（2026-07-29）

| # | 问题 | 说明 |
|---|------|------|
| 1 | 激活面板未显示图标和介绍 | extractRenderState 需要补充 renderItem + 描述文本渲染 |
| 2 | 切换按钮未做边界禁用 | selectedIndex==0 时左按钮禁用，selectedIndex==size-1 时右按钮禁用 |
| 3 | 右键一律打开面板 | useWithoutItem 应恢复之前聊天信息输出，面板仅标记工具右键触发 |
