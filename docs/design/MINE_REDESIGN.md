# 矿井挖矿模块重构设计(Mine Redesign)

> 来源:2026-08-13 ~ 08-14 会话讨论(从本地会话记录恢复,原讨论未留下文档)
> 状态:全部拍板完毕(2026-09-04,A1/B1/B2/B3/C1/C2/D1/D2/D3 共 9 项),待实现 — 历史标注 ✅ 为已拍板,❓ 为待拍板
> 关联:[MINE_SPIRAL_ALGORITHM.md](./MINE_SPIRAL_ALGORITHM.md) | [TODO_V1.md](../issues/TODO_V1.md)

---

## 0. 背景与硬约束

- 目标:**基于工程中心框架完全重设计挖矿模块**(第一版工程设计草案作废)
- ✅ **矿井生成算法不能改**:`SpiralMinePlanner` 及周期/层坐标计算(`computeCycle` 风格的 keeps/lights 计算)原样搬迁,只移动不修改任何坐标公式
- ✅ 挖矿分两条线:
  - **矿井挖矿**(Home 模式):逐步完成矿井建造,走工程中心流程
  - **跟随采矿**(跟随模式):只采掘跟随范围内的表面矿,纯 Behavior,**逻辑独立于矿井**(不是砍树那种拦截气泡,真的能干活的)

## 1. 架构:矿井 = 工程中心子类

✅ 用户拍板:**矿井方块 = 工程中心方块的子类实现,完全走工程中心这一套**

```
ProjectCenterBlock        ← MineBlock(子类方块)
ProjectCenterBlockEntity  ← MineBlockEntity(身份桥 + 容器)
ProjectCenterInstance     ← MineInstance(后台实例,SavedData 持久化)
ProjectCenterManager      ← 能同时驱动两类实例
```

差异点(子类覆写):

| 维度 | 普通中心 | 矿井(子类) |
|---|---|---|
| 几何 | 半径立方体(radius+anchor) | 标记工具两角点 + 奇数边长 |
| 目标来源 | 方块扫描(stateFilter) | 螺旋规划器按周期/层生成任务 |
| 任务模型 | 块目标(原木) | (坐标, DIG/FILL/LIGHT/REPLACE) 四类 |
| 创建流程 | 直接放方块 + 激活面板选类型 | 标记工具选角点 → 确认 → 放矿井方块(类型固定"采矿",无激活面板) |
| 附加能力 | 通用临时仓库 + 困难任务表(A1 拍板,升级为全中心通用,不限格数) | 竖井形状参数 + 螺旋分层(仓库/困难表已上移为通用,见 A1) |

✅ **已拍板(2026-09-04):矿井 = 工程中心子类,复用同一套管理器 + 多态 Codec 分派**
- 工程中心(基类)保持**通用/解耦**:半径区域、存档、每 tick 驱动、给女仆分活、清理。`ProjectCenterManager` 零改动(本就是按基类泛型遍历),仅新增 `createMine` 入口。
- `ProjectCenterInstance` 长期运行,只靠**多态 Codec** 区分"普通中心 / 矿井"(照抄 `ProjectBase.CODEC` 的 `Codec.STRING.dispatch(kind→...)`),旧存档回落 "center"。
- 分界:**通用架子**(存档/派活/清理/区域/仓库/困难表)→ 基类复用;**矿井特有**(竖井构造 + 跟女仆挖矿行为的耦合)→ 子类。

**三条子决定(均已确认):**
1. **区域语义**:中心 `半径+锚点` = 干活边界(挖掘行为只在范围内发生),与伐木中心**完全同义**,照旧复用;**竖井形状参数**(标记工具两角点/螺旋尺寸)不是中心参数,属矿井子类,用于构造竖井(换形状如倒锥则随之改)。
2. **储物 → 通用**:推进工程中心,担任"工程临时仓库";**不限 27 格**,改做面板数据记录(所有中心共享,非矿井专属)。
3. **困难任务表 → 通用**:放入工程中心,因任何工程都可能遇到"当前工具挖不动、留待处理"的情况。

**层级:**
```
ProjectCenterInstance(基类,通用/解耦: 半径+存档+分活+仓库+困难表)
   └── MineInstance(子类,与女仆挖矿行为耦合: 竖井形状参数 + 螺旋分层 + 四类坐标任务 + 索光源/结算中介)
```

## 2. 几何分离 + 深度限制

✅ 用户拍板:矿井边长和工程中心范围**分开处理**(为鱼骨矿道预留)

- **螺旋 L/W**:标记工具两角点定(奇数、`≥7` 下限,与普通中心 `2r+1` 恒奇数同一套几何约束),只喂 `SpiralMinePlanner`
- **强制正方形**(2026-09-04 测试补充拍板):竖井长宽必须一致,两角点框选非正方形时**自动按长边取正、以框选中点居中**;确认窗提示"已按长边取正方形"
- **中心半径/基准点**:独立可编辑(EditHUD 里调),默认覆盖矿区 + 鱼骨预留(实际边长 = 矿井边长 + 两边鱼骨矿道最长距离)
- **深度限制 = 中心盒子底**(anchor=top 时 `y - 2r`):螺旋下挖到该 Y 即"挖到底",停止派发新层

## 3. 工程位 = 一层

✅ 用户拍板:挖矿的工程位是"**一层**",派发时直接把一层的区域范围做成一个 project 创建

```
矿井实例(中心子类)持有一层工程
└─ 当前层 → MiningProject(多人参与,实时破坏方块,非计数型)
   ├─ 任务池:该层所有需处理坐标(区域 ±1 圈)
   ├─ 派发:女仆请求推进 → 无子任务 → 取距她最近未处理坐标 → 判定类型
   │        (挖掘/补充/替换;keep 完好 → 直接跳过不生成子任务)
   ├─ 子任务:女仆 ↔ 单个坐标绑定,完成前不接新任务
   ├─ 离场:子任务取消,坐标退还池子
   └─ 池子空但子任务进行中 → 其他无任务女仆请求 → 让位等待
完成后 → 矿井推进下一层 → 新建层工程
```

- ✅ 该层工程属于**实时更新类型**:完成前不仅更新后台进度,方块也要实际破坏

## 4. 任务四型

✅ 用户拍板:REPLACE 概念保留,四类任务都要真正派发

- **DESTROY / FILL / REPLACE / SETLIGHT** 四类
- 旧代码事实:REPLACE 和 LIGHT 只算了集合(`currentCycleReplaces`/`currentCycleLights`),任务扫描只当排除项,分支从未真正生成任务 → 僵尸设计。新设计全部真正派发
- ✅ 处理顺序:**保留 → 挖掘 → 补充 → 光源**(用户纠正了旧 TODO 的"光源→替换→保留→挖掘")
- 旧的 `currentCycleReplaces`(内圈一周独立集合)概念**作废**;REPLACE 改为任何 keep 位遇到非垫脚实体块时**按需生成**

## 5. REPLACE = 墙壁处理

✅ 用户拍板:REPLACE 的用途是**处理墙壁**

keep 位(墙壁/踏步)必须是"实体垫脚方块",三态处理:

| keep 位状态 | 处理 | 原因 |
|---|---|---|
| 空气/空洞 | FILL 补上 | 防怪物穿墙、防流体漏进矿道 |
| 已是垫脚方块 | 跳过(丢已处理) | 无需处理 |
| 非垫脚实体方块(石头/矿石) | REPLACE 挖掉再垫 | 防玩家看着墙上留矿难受,顺带回收墙上矿 |

## 6. 派发树(唯一真相)

✅ 用户 [if] 编号原文,逐条确认过:

```
女仆请求推进(无绑定子任务时):
  0. 先过困难列表:矿井困难表 → 层工程困难表,有能处理的 → 取走干活
  1. 未处理列表非空 → 取最近坐标 → 在保留列表?
     ├─ 在:无方块 → FILL
     │    有方块 → 垫脚 → 丢已处理,等下次请求
     │             非垫脚 → 能处理?→ 是 → REPLACE 派发
     │                              → 否 → 层困难表,等下次请求
     └─ 不在:能处理?→ 是 → DESTROY 派发
                         → 否 → 层困难表,等下次请求
  2. 未处理空 → 光源列表非空?
     ├─ 有:女仆有光源 → 取最近 → SETLIGHT 派发
     │     无光源 → 跳出,Behavior 驱动她去矿井方块索要
     └─ 空:工程结束 → 有子任务进行中 → 等
                     → 无 → 结算:层困难表上缴矿井,删工程,矿井派发新层工程
```

各工程持有:**子任务列表 / 保留列表 / 未处理列表 / 已处理列表 / 光源位置列表**。

### B1 拍板（2026-09-04）：女仆侧 Home 循环

- 女仆侧新增**专用挖矿 Behavior**(Home 模式下与矿井中心对接),不复用砍树"一次性给全目标/计数"模型
- 循环:女仆空闲 → 向矿井中心要活 → **中心**从当前层任务池挑**距她最近的未占用坐标**、按类型(挖/填/替换/光源)派发 → 女仆装备最好的镐 → 寻路 → 执行 → 完成后再要下一坐标 → 循环 → 整层完成 → 中心结算、推进下一层
- **由中心决定"下一个坐标"**:`MineInstance` 对外只开一个"派活"口(返回"坐标 + 动作类型"或"本层无活"),女仆只与此口交互;螺旋算法/分层/结算全在中心内部(对应上方派发树的就近分配)
- 特殊情况:工具不足 → 该坐标进困难表、换下一格;要放灯但无光源 → 先去中心仓库取;背包满 → 先把矿存进中心仓库再继续
- 本质为**逐坐标执行型**工程(真实破坏/放置),非 CountingProject 计数型
- 落地说明(2026-09-04 实现批次):层任务池暂由 `MineInstance` 直接承载(周期/层索引持久化,层池重载后按周期重算,实时方块状态即进度真相);`MiningProject`(ProjectBase 包装)随女仆行为批次落地
- 挖掘执行(2026-09-04 拍板):**真挖掘**——原版公式逐 tick 推进(进度 += 工具速度÷方块硬度÷(可正确掉落?30:100)),`destroyBlockProgress` 实时裂纹渲染,完成时扣 1 点耐久;速度自然关联工具与方块(徒手挖石≈150tick,石镐≈22tick)
- **七步工具决策流程(2026-09-04 终版拍板,§8)**:**先检查再分配**——可行性五级检查前置到派发侧:①自身推荐(类型匹配+能正确掉落) ②仓库推荐 ③非必须(空手可掉落) ④自身必要 ⑤仓库必要,任一成立→分配;全不成立→**不分配,进层困难表**+记录缺工具数据(基岩类同此)。执行侧收敛为**装备流程**:自身推荐→装备开挖;仓库推荐→取货支线;非必须→空手挖(主手收回背包);自身必要→装备;仓库必要→取货支线——不存在"不可挖掘"执行期出口(仓库竞态兜底:释放重派)
- **气泡规则(2026-09-04 测试修正)**:取货支线(垫脚/工具/流体瓶)全程静默执行,气泡只在"仓库也没货"时冒
- **挖尽 = 待机不轰走(2026-09-04 测试修正)**:矿井挖尽后女仆**保留成员身份**不退出中心,走到矿井方块旁并将 Home 半径临时放宽到 32 自行闲逛;仅被拾起/切任务才真正退中心(修复"反复加入退出中心")
- **挖尽后的维护通道(2026-09-04 拍板)**:挖尽状态不派新层,但 10s 封存检查照跑——落入矿道的沙子/被打掉的灯等违规仍会生成维护任务并派发处理,处理完回待机(修复"放沙子不重启任务"/"火把打掉不补")
- **挖尽维护的任务来源**:上述维护任务直接来自矿井中心维护封存列表的周期检查,不重新开启新层工程;挖尽状态只允许处理这些封存违规任务
- **火把朝向推算+原版校验(2026-09-04 终版)**:灯位必靠某面墙的内侧 1~2 格,支撑方向 = 四向中"到边界最短"的外侧(北→-z/南→+z/西→-x/东→+x,例:井宽11 北边灯1(cx,h,minZ+2)→支撑(cx,h,minZ+1) 方向-z);FACING=支撑方向,放置前原版 canSurvive 校验兜底(不活试反方向,仍不活放弃灯位)——推算为主校验保命,杜绝插空气与朝向错乱
- **缺工具中心 UI(2026-09-04 拍板)**:ProjectCenterInfoPayload 增加缺工具列表字段,中心上方面板渲染"缺少:xxx"(红色);女仆五级全空(真缺)时同步冒一次缺工具气泡(符合"真缺才冒"规则)
- **灯笼不补=预期(只认火把拍板)**:灯笼打掉不补是正确行为;挂顶灯笼后续单独立项(待办:加栅栏挂到下一层)
- **导航熔断(2026-09-04 测试修正)**:导航失败上限 10 次,达到后视为到达继续执行(不取消任务);取货/存货流程同步检查 reachedTarget(此前只查 isNear,熔断后原地反复寻路)
- **流体处理统一修正(2026-09-04 测试修正)**:流体判断提到保留位判断之外——**任意位置的源流体 → REPLACE**(清掉+收流体瓶+**原位放垫脚塞子**);流动流体 → 保留位 FILL(垫脚顶掉)、非保留位跳过(源清完后自然干涸)。先前非保留位水被派 DESTROY:水硬度 100 慢挖且邻水回流,形成"永远挖不完"死循环;行为侧加流体兜底(历史遗留 DESTROY 水条目走流体处理)
- **水源塞子机制(2026-09-04 拍板)**:无限水机制下"取水→邻水回流→再取"极不稳定,故取完水源后**无论保留位/非保留位都在原位放垫脚方块**:保留位=永久墙体,非保留位=**临时塞子**——塞子封存为"应空",由 10s 周期核查滞后发现并挖除;那时邻位水源已被清完不会回流。完成坐标同时记入本层已处理,防止塞子被本层扫描立即重复派发
- **光源位流体处理(2026-09-04 扩展)**:光源位是水源时先前漏处理(水永远清不掉);现光源位流体 → 清掉(源→收瓶)后释放任务,下一轮 SETLIGHT 重派时格已空气即可放火把(层末尾其他水源已清,不会回流)
- **卡死熔断传送(2026-09-04 拍板,同日三次修正)**:女仆卡死 10 秒 → 直接传送到矿井方块旁安全落点并重置导航状态,避免永远卡死。修正 1:初版判定绑在"导航进行中"(isInProgress)上,而水中 moveTo 反复失败会让该状态反复变假、计时反复清零,传送几乎不触发;修正 2:**采样移到矿井侧**(矿井实例 tick,由全局驱动,状态绝对稳定,女仆行为可能因卸载/切换而停走),**每 5 秒采样一次**;修正 3:位移判定仍会被水中漂移/上下浮动反复清零(每 5 秒漂过一格就重置)→ 改为**"距目标无进展"判定**:目标=绑定子任务坐标(无绑定则矿井方块),离目标/中心 ≤4 格(在施工/存取/待机)算正常,比历史最近距离更近算有进展,否则累计满 10 秒 → 下发一次性通知,女仆行为消费后传送并重置导航状态(清任务绑定外的本地导航字段,从中心重新寻路);首次累计 5 秒打一条"卡死采样"诊断日志便于定位
- **空置封存检查修正(2026-09-04 测试修正)**:`checkSealedSlice` 对"应空"位此前漏了空气跳过分支——空气格被当违规反复派发 DESTROY,单场会话刷屏 5000+ 条并灌困难表(派发时被"已解决"过滤,故未实际派给女仆);现空气=正常跳过,塞子/被埋方块等非空气仍照常违规派发
- **活跃任务熔断(2026-09-04 拍板)**:女仆死亡后行为不再 tick,绑定任务只靠行为侧 stop() 释放会永久残留 → `requestWork` 的"层结算"(`layerSubtasks` 非空即等待)将其余女仆全部卡死。现改为:死亡事件(`LivingDeathEvent`)立即归还绑定(困难表来源回坐标池表、取灯除外)+脱离中心;5 秒采样另有兜底——绑定女仆实体不存在连续 2 次(10 秒)→归还任务(失联只归还不除名,可能只是区块卸载);实体在场但已死→立即归还并移出成员表
- **被埋救援(2026-09-04 拍板)**:放方块路径(垫脚/塞子)不做占位检查(会带来女仆被封在水里的风险),改为**矿井侧每 tick** 检测成员 `isInWall()`(原版窒息伤害同款判定)→立即就近传送到"空气+下方实体"安全格,找不到退矿井方块旁安全点,并打"被埋救援"日志
- **水中抖动诊断(2026-09-04 临时接入)**:TLM 自带游泳系统(`MaidNavigationManager` 在陆地/两栖水下两套导航间动态切换、`MaidSwimManager` 切游泳姿态/碰撞箱、`MaidBreathAirTask` 憋不住上浮换气、`MaidSwimJumpTask` 上浮),矿井行为在水里仍尝试下发 `moveTo` → 疑似两套系统在水面附近互相打架(导航对象/姿态/碰撞箱高频切换)导致原地抖动。行为侧加每秒一条诊断日志(速度/水深/姿态/导航类名/TLM 泳态/moveTo 窗口统计),先确定抖动源再定修法
- **矿道层控制方块(2026-09-04 拍板,延伸矿道前置)**:新方块 `mine_layer_control`(方块+方块实体,记录所属矿井 UUID),表示**该层矿道的控制中心**。放置工序:每层"坐标池空 → 火把(光源)完成"后,女仆走到平台**"中心列 × 贴墙行"格**(即灯2 正下方 3 格),把该格垫脚方块替换成控制方块(垫脚物品回收进背包)——**空手生成放置:不消耗物品、无取货环节、不存在缺货**;方块属性=非合成、无掉落、生存不可破坏(创造可破坏)。每周期 4 个(北/东/南/西各一,各自独立层),位置=(cx,h,minZ)/(minX,h,cz)/(cx,h,maxZ)/(maxX,h,cz)
- **控制位验证线(2026-09-04 拍板)**:与光源线同款——完成放置即封存进独立列表 `sealedControls`(持久化,跨周期累积;封存对齐优先级:控制位 > 灯位 > 保留位 > 空置);10s 检查新增第 4 条分片验证线,格上不是控制方块 → 重派 `PLACE_CONTROL`(覆盖创造模式挖除);困难表派发对该类型特判:已解决=格上是控制方块,跳过基岩/工具可行性检查(空手放置)
- **控制方块随矿井中心销毁(2026-09-04 拍板)**:拆除矿井中心方块时(`deleteById`)遍历 `sealedControls` 把仍是控制方块的格置空气(在实例注销前执行);另有控制方块实体每 100 tick 兜底自检——绑定矿井不存在则自毁(覆盖"中心移除时该区块未加载"的边缘情况),不写层/周期/边号(需要时按坐标反推螺旋公式)
- **工程制派活(2026-09-04 拍板)**:矿井接入工程体系——**竖井层=工程**(容量不限)、**横向矿道=工程**(容量 3),两者并行。女仆找活时先续手头工程,否则在"可加入/可新建"候选里随机挑(当前层竖井、有空位的矿道工程、未挖完且无工程的矿道);工程完工(竖井层结算 / 矿道挖格+火把全清)由基类移除并清空分配,女仆下次找活随机再分配。矿道工程跨周期保留(控制方块记录带周期/层/边,存读档恢复;进度按世界方块状态)。挖尽(竖井到底)只停竖井,矿道工程继续,矿道全清才释放成员
- **横向矿道几何(2026-09-04 拍板)**:逐平台生成、朝该边外侧——**纵道** 3 格宽(平台中心列)从平台贴墙行外侧一格挖到矿井范围(锚点±半径)边缘;**横道** 1 格宽,位于纵道往外第 2、5、8…格(首条隔 1 格、其后每条隔 2 格),沿范围宽度贯通;高度 2(层高 h+1/h+2),地板 h 纳入实体封存(10s 检查补洞,防掉洞)
- **矿道火把(2026-09-04 拍板)**:主道中心×横道交叉点各 1 根,横道内自交叉点往两侧每 3 格 1 根;**站立火把插在地板上**(原版 canSurvive 校验脚下支撑);封存进独立列表 `sealedFloorLights`(持久化),10s 检查缺灯重派 `SETLIGHT_FLOOR`;火把同样从仓库取(取灯流程复用)
- **中心 UI 按中心缓存(2026-09-04 测试修正)**:客户端由全局单份信息改为按中心坐标缓存、各自渲染面板——多中心每 20t 轮流广播时不再互相覆盖(闪烁根因:远处中心数据覆盖进来后面板被距离门限隐藏)
- **光源位封存列表(2026-09-04 测试修正)**:放灯完成封存至独立的光源位列表(与空置/实体并列);10s 检查发现灯位空 → 重派 **SETLIGHT** 而非 FILL(修复"打掉灯补成垫脚")
- **光源只认火把(2026-09-04 拍板)**:取灯/放灯/判定收敛为仅火把(灯笼无法贴墙挂放;挂顶灯笼后续单独立项,待办:加栅栏挂到下一层)
- **REPLACE 收窄(2026-09-04 拍板,§5 修正)**:保留位实体方块仅**矿物(#c:ores,含模组矿)**挖掉换垫脚回收;砂岩/石头等其余实体直接跳过不替换
- **矿井定型采矿类型(2026-09-04 拍板,§1 修正)**:新增 MiningProjectType(id="mining"),createMine 时 projectTypeId 直接定型;标记工具右键矿井方块**直达配置页**(类型锁死,调半径/锚点/命名)
- **全阶段切换日志(2026-09-04 拍板)**:女仆所有阶段切换打点([MineDebug] 女仆=id 旧阶段→新阶段 明细),可完整回放行为轨迹(测试期临时接入)
- **火把朝向修正(2026-09-04 测试修正)**:原版 WallTorchBlock.FACING 语义 = 从火把格**指向支撑墙**(canSurvive 以 pos.relative(FACING) 校验),先前方向反了导致火把视觉偏一格且四面镜像
- **移除挖后下方垫脚(2026-09-04 拍板)**:挖穿一格后不再向正下方自动垫脚——下方格属于下层隧道/待挖区域,自动垫会污染封存并被下层重复挖掘("挖4放4"瀑布根源);保留源流体清除+流体瓶与基岩跳过;楼梯为官方通行路径
- **结算封存对齐(2026-09-04 拍板)**:每层结算时以当前周期规划器保留区为真相,重核本层区域所有格子的封存类别(灯位→光源封存/保留格→实体封存/非保留格→空置封存),修正历史误分类(如楼梯格被标"应空"导致 10s 检查误拆楼梯);测试阶段不做旧存档兼容,存量矿井拆掉重建
- **层暂停(2026-09-04 拍板)**:层结算复核后,层困难表剩余不可处理数 > 该层总格数((L+2)×(W+2))的 10% → **该层工程暂停**——停止派发,女仆到工程中心旁等待;矿井每 10 秒对暂停层重判(工具来源=仓库+在场女仆随身),可处理比例回落至 10% 内自动恢复;缺工具数据先行持久化(UI 阶段弹窗展示)
- **角点缓存临时化(2026-09-04 拍板)**:标记工具两角点为会话内存缓存(按玩家记),重登即失效,玩家重新框选
- **挖尽成员释放(2026-09-04 拍板)**:矿井挖尽后释放全部女仆成员并恢复各自 Home,保留矿井方块/实例/仓库;维护任务仍可由封存列表周期检查派发
- **七步工具决策流程(2026-09-04 终版拍板,§8)**:每个目标方块——1.判定类型(可挖掘标签:镐/锹/斧/锄)与是否必须正确工具掉落;2.推荐工具=类型匹配且能正确掉落(候选取对目标速度最高);3.自身(主手+背包)有推荐→装备;4.仓库有推荐→取货支线;5.非必须→直接空手挖(主手收回背包);6.自身必要工具(能正确掉落即可)→装备;7.仓库必要→取货支线;都凑不出→不可挖掘→困难表+失败冷却(补货后重试)。派发侧难度判定降级为基岩拦截,工具可行性判定移到行为侧
- 光源放置(2026-09-04 崩溃修复):不走 `BlockPlaceContext`(需 Player,女仆非 Player),手动放置——**摆放面只检查侧面,不允许上/下两面挂**(放置时下层未挖、地面仍是实体,落地灯会被下层挖掉);火把贴墙(WallTorchBlock.FACING);非火把光源无法贴墙→放弃该灯位

## 7. 困难任务双层结构 + 红点渲染

✅ 用户拍板:

- **层工程困难表**:独立于常规工程子任务的特殊处理;DESTROY/REPLACE 被拒时入表(无法破坏 / 工具等级不够);女仆申请新子任务时**优先**从该表查找,能处理就取走
- **矿井困难表**:层结算时上缴累积;女仆请求任务时**先于一切**过一遍工程中心(矿井)困难表
- 若工程所有可用子任务派发完,困难表仍有无法解决/难以解决的子任务 → 推送到工程中心
- **红点渲染**:玩家手持标记工具且标记工具绑定了该矿井时,矿井困难表所有位置方块标红(客户端渲染)

## 8. 难度判定 — 纯布尔

✅ 用户拍板:不需要分级,只有"能做/不能做"一个 bool

```
不能做 = destroySpeed < 0(基岩类)                 ← 永远不能
     或 方块 requiresCorrectToolForDrops
        且 女仆主手工具 isCorrectToolForDrops == false   ← 铁镐挖黑曜石 → false
其余 → 能做
```

- 女仆开工前先装备最好的镐(复用砍树 `equipAxe` 模式),然后用主手工具判定

## 9. keep 位流体

✅ 用户拍板:

- **源流体**(`state.isSource()`)→ **REPLACE**:setBlock 空气清掉 + 生成对应流体瓶塞(复用旧 `checkBelowSafety` 流体瓶逻辑 + `FluidBottleItem.setFluid`),再垫垫脚方块
- **非源流体**(流动水/岩浆)→ **FILL**:直接在上面放垫脚方块(setBlock 会顶掉流动流体)

## 10. 光源位

✅ 用户拍板：

- 光源位**不进未处理列表**(避免先被 DESTROY 挖了),层末尾 SETLIGHT 阶段单独派发
- 光源位有方块也不进 DESTROY 流程

### 光源位置拍板（2026-09-04 测试修正）

- 光源**只在每条边中段的 3×2 平台**处放,每平台 2 盏(每周期 4 边×2=8 盏):
  - **灯1** = 平台中柱走廊侧外一格,贴平台侧面(平台=保留区,支撑永久)
  - **灯2** = 平台端柱上方**三格(y+3)**,贴边界围墙内面(围墙=保留区,支撑永久;高于墙区的平台跳过灯2)
- **灯位保护**:发光且非流体的方块一格**永不进挖掘池**(防任意层级误挖)
- 旧方案(每层过道行放灯)**作废**:灯踩着的支撑方块属于下层挖掘区,下层一挖支撑没了灯就掉

### B2 拍板（2026-09-04）：索要光源

- **触发**:女仆领到 SETLIGHT 任务、背包无可放置光源时,先去中心取灯(不提前囤灯,不白跑)
- **领取量**:一次取**一组(或 10 个)**防来回跑;仓库不足时**有多少拿多少**(如剩 3 个就拿 3 个);仅当**一个都没有**才判定"取不到"
- **领取方式**:全自动——寻路至矿井方块旁 → 向中心请求 → 仓库扣光源入背包 → 返回坐标放置,无需玩家确认
- **仓库无光源**:女仆发出请求并在工程中心旁**阻塞等待**("发出请求"属后续规划,现阶段以**聊天栏输出提示**替代)
- **光源判定**:任何**可放置且发光**的物品(火把/灯笼/萤石等),仓库有什么用什么

### B3 拍板（2026-09-04）：仓库存取协议

- **存矿触发(测试修正 2026-09-04)**:背包范围**仅背包格**(左手完全不参与,不计数不填充);流程 = 干完手头这格活 → **产物放入背包** → 检查放入后**无空格** → 立即前往仓库存放(到仓时全满为正常);寻路连续失败 3 次 → 气泡提示 + 放弃本次存货继续干活(多余掉落落地),不死等
- **存什么(保留规则,2026-09-04 二次修正)**:镐(工具)不存;垫脚类/光源类**每类随机保留一种、最多保留一格**,其余整类入仓(泥土/圆石既是垫脚也是挖矿产物,不再被黑名单整类误伤导致"只存一格"死循环);其余产物(含流体瓶)全部入库
- **存取实现**:统一走合并容器(availableBackpackInv)读写,避免"遍历基础背包看不到扩展背包产物"的站桩问题
- **取消耗品**:垫脚方块与光源走**同一套领取协议**——一次取一组(或 10 个),有多少拿多少,一个都没有则阻塞等待 + 聊天栏提示(同 B2)
- **玩家面板存取**:留到 UI 阶段;本次只定数据协议——仓库 = 中心持有的一张物品列表,支持女仆存取 + 玩家面板存取

## 11. payload(客户端红框)

✅ 方向已定;字段已拍板(2026-09-04,见 §15 D3):

- 现有 `ProjectCenterInfoPayload`(每 20 tick 全玩家广播、无坐标表)**不塞**困难列表
- 新开一个 payload 传 `(矿井ID, 困难坐标列表)`,服务端在**列表变更或玩家绑定时定向发送**给持标记工具的玩家
- 客户端缓存后按坐标画红框(复用 `WireframeBoxRenderer`)

## 12. 跟随采矿(跟随模式,独立于矿井)

✅ 用户描述(最后一条需求,仅骨架):

> 很简单的纯 Behavior 逻辑:空闲时 10tick CD 在跟随范围内做一次检索,检索到有 ore 与空气相邻且该 ore 可以使用背包里的工具进行采集 → 移动到尽可能近的地方 → 走一个采集流程

✅ 已细化:跟随范围与矿石判定见下方 C1 拍板,采集流程与背包满策略见 C2 拍板(均 2026-09-04)

### C1 拍板（2026-09-04）：跟随范围与矿石判定

- **范围模型**:跟着玩家走、**沿途见矿就挖**(检索以她沿途所及为准);**距玩家超过一定距离(可配置,默认值实现阶段定)→ 直接中断当前挖矿任务、归队跟上**。距离锚点是**玩家**,不是固定搜索半径
- **矿石判定三条件(先按此实现)**:
  1. 矿石标签(tag,数据驱动,兼容模组矿)
  2. 至少一面邻空气(露天,不隔墙挖)
  3. §8 难度布尔(基岩类不挖;需正确工具而手中工具不行不挖)
- **占用跳过**:已被认领的坐标(其他女仆/工程)直接跳过,复用占用快照 `collectOccupied`

### C2 拍板（2026-09-04）：采集流程与背包满策略

- **采集流程复用**:提取"**单坐标执行**"公共例程(寻路到旁 → 装备包里最好的镐 → 挖 → 掉落物进背包),Home 挖矿与跟随采矿**共用同一实现**,不写第三份"挖一格"
- **背包满策略(可配置)**:提供配置项由玩家决定"背包满 → 停止挖矿纯跟随"还是"继续挖";**默认 = 停止**(停止检索新矿、不再挖,背包有空位后自动恢复)
- **不扔地上、不找仓库存**:跟随模式独立于矿井,不与仓库耦合
- **超距归队被打断的矿**:不做特殊处理——仍是露天未占用矿石,下次路过再次检索到即接着挖

## 13. 遗留问题清单

| # | 问题 | 状态 |
|---|---|---|
| A1 | 矿井实例管理器注册表:共用一套 + 类型分发,还是矿井独立一套? | ✅ 子类化 + 共用一套 + 多态 Codec(2026-09-04) |
| B1 | 女仆侧 Home 模式行为流程:如何"请求推进"、挖完确认/结算流程(未展开) | ✅ 专用挖矿 Behavior + 中心派活口 + 就近派发(2026-09-04) |
| B2 | 去矿井方块索要光源的行为细节(触发条件、路径、领取交互) | ✅ 缺灯才取 / 一次取一组(不足拿剩余) / 无灯阻塞等待+聊天栏提示(2026-09-04) |
| B3 | 27 格储物容器 + 存取协议交互细节(何时存矿、背包满策略) | ✅ 剩余格<阈值(默认3)去存 / 黑名单外全存 / 取消耗品复用光源协议 / 玩家面板留 UI 阶段(2026-09-04) |
| C1 | 跟随采矿:跟随范围、ore 判定条件 | ✅ 沿途见矿就挖 / 距玩家超阈值(可配置)中断归队 / 矿石标签+露天+§8难度 / 占用跳过(2026-09-04) |
| C2 | 跟随采矿:采集流程复用哪个流程、背包满怎么办 | ✅ 单坐标执行公共例程(Home/跟随共用,含换镐) / 背包满行为可配置,默认停挖纯跟随(2026-09-04) |
| D1 | TODO_V1 #8 重力方块掉落(沙子/沙砾掉进已挖空区域)是否纳入本次重构 | ✅ 纳入,层内周期全量重扫(间隔可配置),偏离规划即入队,防沙堵楼梯跳楼(2026-09-04) |
| D2 | 矿井挖到底(无新层可派)后实例终态、容器处理 | ✅ "已挖尽"标记终态,实例/仓库保留,拆方块才删(2026-09-04) |
| D3 | 困难表红点 payload 具体字段设计 | ✅ (矿井ID+坐标列表)定向推送,变更/绑定触发,客户端缓存渲染,解绑清空(2026-09-04) |

## 14. 关联 TODO

- #6 光源列表 / #22 替换列表 → 本次重构解决
- #20 keep 位含矿石先挖后填 → 即 REPLACE,已纳入
- #2 就近分配 → 派发树已定(取最近坐标)
- #8 重力方块掉落 → 见遗留 D1
- 鱼骨矿道(FishboneMinePlanner)→ 未来项,本次只影响几何预留(见 §2)
- #8 重力方块掉落 → 已纳入,见 §15 D1

## 15. D 组拍板（2026-09-04）

### D1:重力方块掉落 — 纳入,层内周期**全量**重扫

**终版拍板(2026-09-04 测试迭代):**

- **2 秒层复核**:层坐标池清空、无人干活时**不立即结算**,先等 40 tick(2 秒)再复核——期间派发扫描实时重判,掉下来的沙砾自然被捞成新任务;2 秒后池子仍空才推进下一层(任何一格完成都会重置计时)
- **封存列表**:女仆完成一格即按动作分类封存(DESTROY→空置封存;FILL/REPLACE/SETLIGHT→实体封存,检查标准"非空"故火把也封得住);两列表由矿井实例持久化维护
- **10 秒记录位置检查**:矿井实例每 10 秒直接检查封存列表——空置位有方块→困难表(DESTROY);实体位为空→困难表(FILL);**分片轮询**(每轮每列表 256 格)保证单 tick 成本恒定
- **矿井自带"带类型"困难表**(§7 调整):条目=坐标+动作类型(FILL 也能入表),持久化;层结算上缴/行为侧不可挖掘/10s 检查违规都进此表;条目完成才移除,判定仍不可挖掘的坐标进失败冷却(补货后可重试);基类通用困难表保留给伐木等其他工程
- 旧方案(每层全量重扫派发池)由上述机制替代:2s 复核管层内掉落,10s 封存检查管"已处理区域后放置的方块"

### D1:重力方块掉落 — 纳入,层内周期**全量**重扫(历史方案,已被终版替代)

- 当前层每隔一段时间(间隔可配置)对**当前层全区域**做一次重校验,任何与规划预期不符的坐标重新入队:
  - 多出来的方块(掉进矿道的沙/沙砾等) → DESTROY
  - 缺失/不对的保留位 → FILL / REPLACE
- **必须全量扫,不能只扫"应空未空"**:沙子若堵在楼梯上,寻路会把下楼变成跳楼——保留位/踏步同样在重扫范围内
- 不监听方块事件;连锁掉落由下一轮重扫自然收敛;只扫当前层,成本可控

### D2:挖到底终态 — "已挖尽"标记

- 无新层可派 → 矿井标记"**已挖尽**":停止派发新层,信息面板/HUD 显示已挖尽
- **实例与仓库保留**(玩家仍可存取),女仆自然空闲
- 只有玩家**拆掉矿井方块**才真正删除实例,不自动消失(留改形状/加深重来的余地)

### D3:困难表红点 payload 字段

- 包 = `矿井 ID + 困难坐标列表`(仅此两字段);现有 20tick 广播包不塞坐标表
- 服务端触发:①困难表变更(进/出表) ②玩家绑定标记工具到该矿井 → **定向推送**给持该工具玩家
- 客户端:缓存 `矿井 → 坐标集合`,复用线框渲染器画红框;解绑/换工具/收到空列表 → 清空
