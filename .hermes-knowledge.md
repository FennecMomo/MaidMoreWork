# MaidMoreWork 遗留问题

## 1. mine_block 动态可见性渲染未实现
- 期望：手持标记工具时才显示烟熏炉模型，不持有时完全不可见
- 当前：方块始终可见（烟熏炉模型），手持标记工具有碰撞可右键交互，不手持无碰撞可穿过
- 尝试过的方案：
  - DynamicBlockStateModel + collectParts检查手持物品 → collectParts缓存不刷新
  - RenderLevelStageEvent 每帧setBlockDirty → 性能差+渲染刷新不生效
  - BlockEntityRenderer → MC26 API大改（两泛型+RenderState模式），无法直接用
- 后续方向：等NeoForge生态成熟后参考其他模组的动态可见方块实现

## 2. GUI弹窗未实现
- 矿井确认创建 → 用聊天栏可点击消息 [确认] [取消] 替代
- 矿井管理（删除/编辑） → 用聊天栏可点击消息 [删除] [编辑] 替代
- 等待 momolib 或 maidmorework 有现成的确认弹窗模板后再做

## 3. 矿井范围线框渲染未实现
- 期望：手持标记工具时显示矿井立方体线框
- 原因：MC26的 RenderType API 大改（无 LINES 常量、RenderType.create 签名变化），Forge 时代代码无法直接移植
- 参考：Structurize 的 WorldRenderMacros.renderLineBox 核心逻辑可用，但 RenderType 创建需适配 MC26
